language: python
python: 
    - "3.5"
    - "3.5dev"
    - "nightly"

install: 
    - pip install coveralls
    - pip install -r requirements.txt

script: 
    - coverage run -m unittest

after_success:
    coveralls

matrix:
  allow_failures:
      - python: "3.5dev"
      - python: "nightly"
